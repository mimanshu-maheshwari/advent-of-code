use super::Result;
pub fn solve(_input: String) -> Result<String> {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::solve;
    #[test]
    fn example() {
        let input = "";
        let expected = "";
        let actual = solve(input.to_owned()).expect("Failed to solve input.");
        assert_eq!(expected, actual);
    }
}
